{
  // https://nuxt.com/docs/guide/concepts/typescript
  "extends": "./.nuxt/tsconfig.json",

  // Ignore the submodules directory so the type-checking doesn't affect it.
  "exclude": [
    "submodules"
  ]
}
